package com.email.microservice.utils

import org.slf4j.Logger
import org.slf4j.LoggerFactory

internal object MailLogger {

    internal val log: Logger = LoggerFactory.getLogger(MailLogger::class.java)

}